Minimal Node/Express server for the Bookstore system

How to Setup:

1. From PowerShell, install dependencies:

```
npm install
```

2. Start the server:

```powershell
npm start
# server will run on http://localhost:3000
```

Notes:
- This server is for local development/demo only.It is not ready for deployment. It will only run on local server.
